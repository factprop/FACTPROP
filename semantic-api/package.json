{
  "name": "factprop-semantic-api",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "dev": "node scripts/local.mjs",
    "build": "wrangler deploy --dry-run --outdir dist",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.34.0"
  }
}
